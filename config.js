export const config = {
  inviteCode: {
    length: 12, // Length of generated invite codes
    maxServers: 10, // Maximum number of servers per invite code
    maxAccesses: 1000, // Maximum number of accesses per invite code
  }
};